import { FiltroSearchPipe } from './filtro-search.pipe';

describe('FiltroSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
